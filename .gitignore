__pycache__/
*.pyc
*.egg-info/
build/
dist/
.ipynb_checkpoints/
# local data + caches (fetched via data/fetch_*.py; see DATA.md)
/data/boss
# local-neighborhood reconstructions (CF4/BORG/Manticore; fetched via data/fetch_cf4.py etc.)
data/local/*
!data/local/README.md
/output/
# latex build artifacts
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
paper/*Notes.bib
.pytest_cache/
.DS_Store

# BOSS veto mangle masks (~1GB, downloaded from SDSS SAS; cached analytic selection is committed instead)
data/boss/veto/

# local-neighborhood completed catalogs (regenerable via pipeline/build_local_release.py; large in full mode)
data_release/local/*.npz
